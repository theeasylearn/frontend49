1) Create nodejs server that return basic information about below city using only html 
        bhavnagar
        ahmedabad
        baroda
        surat 
        rajkot
2) Create nodejs server that returns 100 dishes as json array of object. 
        each dish has name, weight, price, ingridents

3) create local module currency.js that has below function
        toDollar(rupees) return dollar of given rupees
        toPound(rupees) return pound of given rupees
        toEuro(rupees) return euro of given rupees

4) create nodejs exam that return 
        all product when request is received for products
        filtered product whose price less then given value for example product?lower=1000
        filtered product whose price greater then given value for example product?upper=1000
        filtered product whose price between given range for example product?lower=1000&upper=5000

5) create web page that accept below details
        Product Name 
        Product Price 
        Product weight
        product Description (text area )
        product size (select)
store that data into file products.txt using nodejs and fs module. 
6) create multi page website for nursery(flowers) using express js 
        at least 6 pages 
        developed using bootstrap 
7) create API that works upon list of object (JSON) that will create/update/delete/fetch account data.
        account should have account id (auto generated), name, balance, mobile,email 
8) create API that works upon list of object (JSON) that will create/update/delete/fetch Product 
        Product should have id(auto generated) name, price, quantity, weight, Size, Description
----------------------------------------------------------------------------------------------------
API 
        Product 
                Add, edit, delete, view, search products
        table 
                id,category,title,detail,price,stock,weight,size,photo,islive,isdeleted

        orders
                view, process, search, Bill generate,print orders
        table 
                1 cart
                        id,productid,quantity,price,userid,billid
                2 bill
                        pincode,shipping (exercise)
                id,userid,billdate,amount,paymentmode,paymentstatus,fullname,address1,address2,city,pincode,mobile,remarks,orderstatus
        //checkout 
        //process
        //check all the items in order are in stock or not 
        //calculate total bill amount 
        //reduce the stock of items given in order 
        insert new record into bill table 
        userid,billdate,amount,paymentmode,fullname,address1,address2,city,pincode,mobile,remarks
        //update cart 
        Users management   (class)      
        view, search, shopping history, send mail, block & unblock   
        users
                id,email,mobile,password,created_at,islive,isdeleted
        Dashboard (class)
       
        -----------------------------------------------	
        register, login, logout, change password, recover account
        browse products (shop), manage cart, place orders 

        (excercise)
                wishlist
        pincode check 
        view history & download bill 
        rating and reviews 

--------------------------
pug 
1) create pug file to display IPL point table 
2) create pug file to display your resume 
3) create pug file to display 5 student 3 subject marks 

Create website for college using pug which should below pages (create pug files that contains common code for all below files, include it in all pugs files using include statement)
  home 
  about us 
  Courses (10 courses)
  alumini (10 alumini)
  contact us 